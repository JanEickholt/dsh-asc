{
  "name": "dsh-compaction-agentic",
  "version": "0.1.0",
  "private": true,
  "description": "Agentic Surface Compaction (ASC) for DeepSeek Harness — the model decides when and what to compact, on top of the event-sourced session log",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "clean": "pnpm -r run clean"
  },
  "engines": {
    "node": "^22.19 || >=24"
  },
  "packageManager": "pnpm@10.13.1",
  "devDependencies": {
    "@deepseek-ai/dsh-fs": "0.1.0-rc.6"
  }
}
